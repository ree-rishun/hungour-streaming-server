apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: LoadBalancer  # 外部アクセス可能にする
  selector:
    app: app
  ports:
    - protocol: TCP
      port: 80        # 外部公開するポート
      targetPort: 8080 # コンテナのポート
